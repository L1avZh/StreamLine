[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "streamline"
version = "3.0.0"
description = "Private, self-hosted chat with a guided CLI and a local web interface."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "L1avZh" }]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Environment :: Web Environment",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Communications :: Chat",
]
dependencies = [
    "rich>=13.7,<15",
    "click>=8.1,<9",
    "fastapi>=0.115,<1",
    "uvicorn>=0.30,<1",
    "websockets>=13,<18",
    "platformdirs>=4.0,<5",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-timeout>=2.3",
    "trustme>=1.1",
    "httpx>=0.27",
    "mypy>=1.10",
    "ruff>=0.6",
]
packaging = [
    "pyinstaller>=6.0",
    "build>=1.0",
    "twine>=5.0",
]

[project.scripts]
streamline = "streamline.cli:cli"

[project.urls]
Repository = "https://github.com/L1avZh/StreamLine"

[tool.setuptools.packages.find]
include = ["streamline*"]

[tool.setuptools.package-data]
streamline = ["web/static/*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
timeout = 20

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = true
files = ["streamline"]
